namespace ProjectEuler

open System

module Problem0051 =
    //First of, we need a relatively quick way to check if a number is prime or not
    open ProjectEuler.Common.Primality
    
    let generateSwappedDigitsNumbers n =
        let length = Seq.length n
        
    let Problem51 =
        [1 .. Int32.MaxValue] |> 